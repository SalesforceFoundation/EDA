@isTest
private with sharing class SObjectFieldMapper_TEST {
    // get all SObjectModels for phone fields with non-phone fields in the mix
    // get empty map when fields exist, but none are of type phone
}
